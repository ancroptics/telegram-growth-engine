"""Watermark service placeholder."""
import logging

logger = logging.getLogger(__name__)


async def add_watermark(image_bytes: bytes, text: str) -> bytes:
    """Future: Add watermark to images."""
    return image_bytes
