"""Batch approve pending join requests."""
import logging

logger = logging.getLogger(__name__)


async def batch_approve_pending():
    """Future: Batch approve all pending requests for a channel."""
    pass
